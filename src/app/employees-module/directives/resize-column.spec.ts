import { ResizeColumnDirective } from './resize-column.directive';

describe('WidthChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizeColumnDirective();
    expect(directive).toBeTruthy();
  });
});
